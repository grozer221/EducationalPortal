export type EducationalYear = {
    id: string,
    name: string,
    dateStart: string,
    dateEnd: string,
    isCurrent: boolean,
    // subjects: Subject[],
    createdAt: string,
    updatedAt: string,
}
